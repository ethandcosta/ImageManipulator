package model;

/**
 * This interface represents Operations that can be expressed as a filter transformation which
 * requires a filter, a "kernel", which is then matrix multiplied to the rgb Pixel values.
 */
public interface FilterableOperations extends Operations {

}
